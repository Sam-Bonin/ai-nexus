/**
 * Electron Preload Script
 *
 * This script runs before the renderer process loads.
 * It has access to both Node.js APIs and the DOM.
 *
 * Currently minimal - may be extended later with contextBridge APIs if needed.
 */

// Empty for now - can add contextBridge APIs here if needed in the future
console.log('Preload script loaded');
